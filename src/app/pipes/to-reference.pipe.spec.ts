import { ToReferencePipe } from './to-reference.pipe';

describe('ToReferencePipe', () => {
  it('create an instance', () => {
    const pipe = new ToReferencePipe();
    expect(pipe).toBeTruthy();
  });
});
